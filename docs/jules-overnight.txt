# Jules Overnight Batch — B/CONTENT
# Run: ./scripts/jules-cli.sh delegate "github:MSCHKY/B-CONTENT" docs/jules-overnight.txt

Add comprehensive Playwright E2E tests for all API endpoints. Create a new test file `tests/api.spec.ts` following the patterns in `tests/smoke.spec.ts`. Test the following endpoints: POST /api/generate (text generation), POST /api/generate/image (image generation), POST /api/generate/website-article (website article generation), GET /api/knowledge/topics (returns all 13 topic fields), GET /api/knowledge/quotes (returns all quotes), GET /api/knowledge/rules (returns content rules), GET /api/posts (lists saved posts), POST /api/posts (saves a new post), DELETE /api/posts/:id (deletes a post). For each endpoint test: happy path with valid input, error case with missing/invalid parameters, response shape validation. Use the existing smoke.spec.ts test setup patterns. Follow AGENTS.md rules.
---
Enable TypeScript strict mode and fix all resulting errors. In tsconfig.json, set "strict": true. Fix all TypeScript errors that appear. Do NOT use `any` type — use proper interfaces from `src/app/types/`. Do NOT suppress errors with @ts-ignore. Run `npx vite build` to verify zero errors. Expected: Clean build with strict mode enabled. Follow AGENTS.md coding rules.
---
Add JSDoc documentation to all exported functions, types, and components. Scope: worker/services/gemini.ts, worker/services/prompt-builder.ts, worker/routes/generate.ts, worker/routes/posts.ts, worker/routes/knowledge.ts, src/app/stores/*.ts, src/app/components/ui/*.tsx. Use standard JSDoc format with @param, @returns, @example where applicable. Keep descriptions concise but informative. English only. Follow AGENTS.md rules.
---
Audit and improve error handling across all API routes and services. Focus on worker/services/gemini.ts (API key missing, rate limiting 429, network timeouts, invalid response format, image generation failures), worker/routes/generate.ts (input validation, meaningful error messages, no unhandled promise rejections), worker/routes/posts.ts (D1 query errors, proper 404 for missing posts, input sanitization). Create consistent error format: { error: string, code?: string }. Add try-catch blocks where missing. Follow AGENTS.md rules.
---
Add input validation to all API endpoints. Create a validation utility in worker/services/validation.ts. Validate all POST body fields: required field checks, string length limits matching PRODUCT_SPEC.md char ranges, enum validation for instance_id ('alex' | 'ablas' | 'bwg'), enum validation for content_type per instance. Sanitize text inputs. Return typed validation errors: { error: string, field: string }. Apply to POST /api/generate, POST /api/generate/image, POST /api/posts. Full TypeScript types required. Follow AGENTS.md coding rules.
---
Create comprehensive API documentation in docs/API.md. Document every endpoint with: Method + Path, Description, Request body (with TypeScript interface), Response body (with TypeScript interface), Example curl command, Error responses. Endpoints: POST /api/generate, POST /api/generate/image, POST /api/generate/website-article, GET /api/knowledge/topics, GET /api/knowledge/quotes, GET /api/knowledge/rules, GET /api/posts, POST /api/posts, DELETE /api/posts/:id. Use English. Follow AGENTS.md rules.
